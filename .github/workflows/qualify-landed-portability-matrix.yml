name: Qualify Workbench portability matrix

on:
  push:
    branches:
      - maintenance/qualify-landed-portability-matrix

permissions:
  contents: read

jobs:
  qualify:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest
    runs-on: ${{ matrix.os }}
    env:
      EXPECTED_SOURCE: 8f2858f1ab5acbac047875bc3590eb231d10d210
      HUSKY: 0
    steps:
      - name: Check out exact public source
        uses: actions/checkout@v4
        with:
          ref: 8f2858f1ab5acbac047875bc3590eb231d10d210
          fetch-depth: 1

      - name: Verify exact source identity
        shell: pwsh
        run: |
          $actual = (git rev-parse HEAD).Trim()
          if ($actual -cne $env:EXPECTED_SOURCE) {
            throw "Expected $($env:EXPECTED_SOURCE), got $actual"
          }

      - name: Set up Node 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Enable pinned pnpm
        shell: pwsh
        run: |
          corepack enable
          corepack prepare pnpm@11.20.0 --activate
          node --version
          pnpm --version
          pwsh --version

      - name: Install dependencies
        shell: pwsh
        run: pnpm install --frozen-lockfile

      - name: Run Developer Mode lifecycle regression
        shell: pwsh
        run: |
          $env:BABEL_ENV = 'test'
          $env:TEST = 'test'
          pnpm exec jest test/common/developer-mode.test.js --runInBand

      - name: Lint portability and lifecycle files
        shell: pwsh
        run: pnpm exec eslint src/background/utils/developer-mode.js src/common/developer-mode.js test/common/developer-mode.test.js tools/validate-workbench-dev-artifacts.js

      - name: Build Workbench Firefox development artifact
        shell: pwsh
        run: pnpm build:workbench:firefox

      - name: Build Workbench Chromium development artifact
        shell: pwsh
        run: pnpm build:workbench:chromium

      - name: Validate built artifact identities directly
        shell: pwsh
        run: node tools/validate-workbench-dev-artifacts.js

      - name: Record OS-specific evidence
        shell: pwsh
        run: |
          $identity = Get-Content -Raw dist/workbench-identity.json | ConvertFrom-Json
          if ($identity.status -cne 'success') { throw 'Portable identity validator did not report success.' }
          $evidence = [ordered]@{
            schemaVersion = 1
            status = 'PASS'
            sourceCommit = $env:EXPECTED_SOURCE
            runnerOs = $env:RUNNER_OS
            runnerArch = $env:RUNNER_ARCH
            workflow = $env:GITHUB_WORKFLOW
            runId = $env:GITHUB_RUN_ID
            runAttempt = $env:GITHUB_RUN_ATTEMPT
            node = (node --version).Trim()
            pnpm = (pnpm --version).Trim()
            powershell = ($PSVersionTable.PSVersion.ToString())
            firefoxExtensionId = $identity.firefox.extensionId
            chromiumExtensionId = $identity.chromium.extensionId
            checks = @(
              'exact-source-identity',
              'developer-mode-lifecycle-regression',
              'portable-validator-lint',
              'workbench-firefox-build',
              'workbench-chromium-build',
              'portable-artifact-identity-validation'
            )
            claims = [ordered]@{
              extensionLifecyclePortable = $true
              developmentBuildPortable = $true
              artifactIdentityValidatorPortable = $true
              nativeHostIntegration = $false
              controllerIngressPortable = $false
              browserE2EQualified = $false
              authorityChange = $false
            }
          }
          $name = "portability-$($env:RUNNER_OS)-$($env:RUNNER_ARCH).json"
          $evidence | ConvertTo-Json -Depth 6 | Set-Content -Encoding utf8 $name

      - name: Upload immutable OS evidence
        uses: actions/upload-artifact@v4
        with:
          name: portability-${{ runner.os }}-${{ runner.arch }}-${{ env.EXPECTED_SOURCE }}
          path: |
            portability-*.json
            dist/workbench-identity.json
            dist-mv3/workbench-identity.json
          if-no-files-found: error
          retention-days: 14
